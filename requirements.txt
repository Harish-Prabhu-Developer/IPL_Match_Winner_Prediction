Flask==2.3.2       # Flask for building the web app
pandas==2.1.1      # For handling and processing tabular data
scikit-learn==1.3.1 # For working with the trained model and using `predict_proba`
joblib==1.3.2      # For loading the model saved with joblib
